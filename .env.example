# ==============================================================================
# FlexPay Scraper Configuration Example
# ==============================================================================
# Copy this file to .env and update the values according to your needs
# 
# Required: GEMINI_API_KEY must be set for the scraper to work
# Optional: All other variables have sensible defaults
# ==============================================================================

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# Google Gemini API Key (REQUIRED)
# Get your free API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_actual_gemini_api_key_here

# ==============================================================================
# BASIC SCRAPING CONFIGURATION
# ==============================================================================

# Target website domain (without https://)
# Example: flexpay.io, example.com, mywebsite.org
BASE_URL=flexpay.io

# Output directory for all generated files
# Will be created if it doesn't exist
OUTPUT_DIR=output

# Test mode: Number of pages to scrape for testing
# Set to empty or remove line for unlimited (production mode)
# Recommended: 5-15 for testing, remove for full scraping
TEST_PAGES_LIMIT=15

# ==============================================================================
# BROWSER & CRAWLING CONFIGURATION
# ==============================================================================

# Run browser in headless mode (no visible browser window)
# Set to false for debugging or if you want to see the browser
HEADLESS_MODE=true

# Number of concurrent crawler threads
# Higher = faster but more resource intensive
# Recommended: 10-30 depending on your system and target server
CRAWLER_CONCURRENCY=20

# Maximum requests per second to target server
# Lower values are more respectful to the target server
# Recommended: 5-15 requests/second
CRAWLER_HITS_PER_SEC=10

# Delay between individual scrape requests (seconds)
# Helps prevent overwhelming the target server
# Recommended: 0.1-1.0 seconds
SCRAPE_DELAY=0.5

# ==============================================================================
# AI/GEMINI CONFIGURATION
# ==============================================================================

# Delay between Gemini API calls (seconds)
# Helps prevent rate limiting and quota exhaustion
# Recommended: 2-5 seconds (free tier has stricter limits)
GEMINI_DELAY=4

# Gemini model to use for content analysis
# Options: gemini-2.5-flash-lite, gemini-1.5-flash, gemini-1.5-pro
# Note: Different models have different costs and capabilities
GEMINI_MODEL=gemini-2.5-flash-lite

# AI creativity/randomness level (0.0 - 1.0)
# Lower = more consistent, Higher = more creative
# Recommended: 0.1-0.3 for factual content extraction
GEMINI_TEMPERATURE=0.1

# Maximum tokens in AI response
# Higher = more detailed analysis but slower and more expensive
# Recommended: 500-1500 depending on your needs
GEMINI_MAX_TOKENS=1000

# ==============================================================================
# ADVANCED CONFIGURATION
# ==============================================================================

# Custom user agent string (optional)
# Leave empty to use default crawler user agent
# USER_AGENT=Mozilla/5.0 (compatible; FlexPayScraper/1.0)

# Custom request headers (optional, JSON format)
# Example: {"Accept-Language": "en-US,en;q=0.9"}
# CUSTOM_HEADERS={}

# Timeout for HTTP requests (seconds)
# Higher values for slow websites, lower for faster scraping
# REQUEST_TIMEOUT=30

# Maximum file size to process (MB)
# Prevents processing of very large pages that might cause issues
# MAX_FILE_SIZE_MB=10

# ==============================================================================
# DEBUGGING & LOGGING
# ==============================================================================

# Enable verbose logging (true/false)
# Set to true to see detailed progress information
VERBOSE_LOGGING=false

# Log level: DEBUG, INFO, WARNING, ERROR
# DEBUG shows everything, ERROR shows only errors
LOG_LEVEL=INFO

# Save raw HTML files for debugging (true/false)
# Warning: This can use significant disk space
SAVE_RAW_HTML=false

# ==============================================================================
# EXAMPLE CONFIGURATIONS FOR DIFFERENT USE CASES
# ==============================================================================

# FAST TESTING (Quick validation):
# TEST_PAGES_LIMIT=5
# CRAWLER_CONCURRENCY=30
# SCRAPE_DELAY=0.1
# GEMINI_DELAY=2

# CONSERVATIVE (Respectful to servers):
# CRAWLER_CONCURRENCY=5
# CRAWLER_HITS_PER_SEC=3
# SCRAPE_DELAY=1.0
# GEMINI_DELAY=5

# PRODUCTION (Balanced speed and reliability):
# TEST_PAGES_LIMIT=
# CRAWLER_CONCURRENCY=15
# CRAWLER_HITS_PER_SEC=8
# SCRAPE_DELAY=0.3
# GEMINI_DELAY=3

# HIGH QUALITY (Detailed analysis):
# GEMINI_MODEL=gemini-1.5-pro
# GEMINI_MAX_TOKENS=2000
# GEMINI_TEMPERATURE=0.2
